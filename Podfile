# Uncomment the next line to define a global platform for your project
platform :ios, '17.0'

target 'MoyaNetworking' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MoyaNetworking
  pod 'Moya', '~> 15.0.0'
  
  target 'MoyaNetworkingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  # If you have a UI Testing target, it should be defined similarly to the unit testing target:
  # target 'MoyaNetworkingUITests' do
  #   inherit! :search_paths
  #   # Pods for UI testing
  # end

end
